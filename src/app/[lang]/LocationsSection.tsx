'use client';
import React from 'react';

interface LocationsSectionInterface {
  pageContent: any;
  lang: any;
}

const LocationsSection = ({ pageContent, lang }: LocationsSectionInterface) => {
  return <section>LocationsSection</section>;
};

export default LocationsSection;
